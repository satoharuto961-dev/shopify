{% assign product = all_products['mental-health-workbook-3-books-in-1-cognitive-and-dialectical-behavioral-therapy-acceptance-and-commitment-therapy-cbt-dbt-act'] %}
{% assign available_variant = product.first_available_variant %}
{% assign is_available = false %}
{% if product and product.available and available_variant and available_variant.available %}
  {% assign is_available = true %}
{% endif %}
<div class="therapist-landing-page">
  <style>
    .therapist-landing-page {
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      color: #1f2933;
      background: #f7f8fa;
      padding-bottom: 4rem;
    }
    .therapist-landing-page .container {
      max-width: 1080px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }
    .therapist-landing-page h1,
    .therapist-landing-page h2,
    .therapist-landing-page h3 {
      color: #101620;
    }
    .hero {
      display: grid;
      gap: 2rem;
      align-items: center;
      padding: 4rem 0 2rem;
    }
    .hero-topline {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 0.75rem;
      font-size: 0.95rem;
      font-weight: 600;
      color: #1d4ed8;
    }
    .hero-price-pill {
      background: #1d4ed8;
      color: #fff;
      padding: 0.3rem 0.85rem;
      border-radius: 999px;
      font-size: 0.95rem;
      font-weight: 700;
      letter-spacing: 0.01em;
    }
    .hero-image img {
      width: 100%;
      height: auto;
      border-radius: 12px;
      box-shadow: 0 20px 45px rgba(15, 23, 42, 0.15);
      background: #fff;
    }
    .hero-content h1 {
      font-size: clamp(2.4rem, 5vw, 3.2rem);
      margin-bottom: 1rem;
      line-height: 1.2;
    }
    .hero-content p {
      font-size: 1.1rem;
      line-height: 1.7;
      margin-bottom: 1.5rem;
      color: #475569;
    }
    .hero-offer {
      background: #fff;
      border-radius: 16px;
      padding: 1.5rem;
      box-shadow: 0 18px 40px rgba(15, 23, 42, 0.12);
      display: inline-flex;
      flex-direction: column;
      gap: 0.75rem;
      border: 1px solid #e2e8f0;
      margin-bottom: 1.5rem;
    }
    .hero-price {
      font-size: 1.6rem;
      font-weight: 700;
      color: #0f172a;
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
    }
    .hero-price span {
      font-size: 0.95rem;
      font-weight: 600;
      color: #1d4ed8;
    }
    .hero-license-note {
      font-size: 0.9rem;
      color: #475569;
      margin: 0;
    }
    .hero-license-note a {
      color: #2563eb;
      font-weight: 600;
      text-decoration: none;
    }
    .hero-license-note a:hover,
    .hero-license-note a:focus {
      text-decoration: underline;
    }
    .buy-now {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.9rem 2.5rem;
      background: #2563eb;
      color: #fff;
      border: none;
      border-radius: 999px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.2s ease;
      text-decoration: none;
      min-width: 200px;
    }
    .buy-now[disabled] {
      cursor: not-allowed;
      background: #94a3b8;
    }
    .buy-now:not([disabled]):hover,
    .buy-now:not([disabled]):focus {
      background: #1d4ed8;
    }
    .buy-now__message {
      margin-top: 0.75rem;
      color: #dc2626;
      min-height: 1.2rem;
      font-size: 0.95rem;
    }
    .cta-reassurance {
      font-size: 0.9rem;
      color: #475569;
      margin: 0;
    }
    .hero-guarantee {
      margin-top: 1rem;
      background: #ecfdf5;
      color: #047857;
      border-radius: 12px;
      padding: 0.9rem 1rem;
      font-size: 0.95rem;
      font-weight: 500;
      display: inline-flex;
      align-items: flex-start;
      gap: 0.5rem;
    }
    .hero-guarantee strong {
      font-weight: 700;
    }
    .hero-guarantee {
      margin-top: 1rem;
      background: #ecfdf5;
      color: #047857;
      border-radius: 12px;
      padding: 0.9rem 1rem;
      font-size: 0.95rem;
      font-weight: 500;
      display: inline-flex;
      align-items: flex-start;
      gap: 0.5rem;
    }
    .hero-guarantee strong {
      font-weight: 700;
    }
    .hero-trust {
      display: flex;
      flex-wrap: wrap;
      gap: 0.75rem;
      margin-top: 1rem;
      font-size: 0.9rem;
      color: #64748b;
    }
    .hero-trust span {
      display: flex;
      align-items: center;
      gap: 0.35rem;
    }
    .sample-link {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      margin-top: 0.5rem;
      font-size: 0.95rem;
      font-weight: 600;
      color: #1d4ed8;
      text-decoration: none;
      padding: 0.55rem 0.95rem;
      background: #e0e7ff;
      border-radius: 999px;
      box-shadow: 0 10px 20px rgba(37, 99, 235, 0.15);
    }
    .sample-link svg {
      width: 16px;
      height: 16px;
      fill: currentColor;
    }
    .sample-link:hover,
    .sample-link:focus {
      background: #c7d2fe;
      color: #1e3a8a;
    }
    .benefits {
      background: #fff;
      padding: 3rem 0;
      border-radius: 12px;
      margin: 3rem auto;
      box-shadow: 0 10px 30px rgba(15, 23, 42, 0.08);
    }
    .benefits h2 {
      text-align: center;
      margin-bottom: 1.5rem;
      font-size: 1.8rem;
    }
    .benefit-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 1.5rem;
      margin-top: 2rem;
    }
    .benefit-card {
      background: #f8fafc;
      border-radius: 10px;
      padding: 1.5rem;
      box-shadow: 0 6px 18px rgba(15, 23, 42, 0.05);
    }
    .benefit-card h3 {
      font-size: 1.1rem;
      margin-bottom: 0.5rem;
      color: #1f2937;
    }
    .benefit-card p {
      margin: 0;
      color: #475569;
      line-height: 1.6;
    }
    .benefits blockquote {
      margin: 2rem auto 0;
      max-width: 640px;
      font-style: italic;
      color: #1f2933;
      text-align: center;
    }
    .benefits blockquote span {
      display: block;
      margin-top: 0.5rem;
      font-weight: 600;
      color: #2563eb;
    }
    .gallery {
      margin: 3rem 0;
    }
    .gallery h2 {
      text-align: center;
      margin-bottom: 1.5rem;
    }
    .inside-columns {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5rem;
      text-align: left;
    }
    .inside-columns h3 {
      font-size: 1.15rem;
      margin-bottom: 0.75rem;
    }
    .inside-columns p {
      margin: 0 0 0.75rem;
      color: #475569;
      line-height: 1.6;
    }
    .inside-bullets {
      margin: 0;
      padding-left: 1.1rem;
      color: #334155;
      display: grid;
      gap: 0.35rem;
      font-size: 0.95rem;
    }
    .inside-highlights {
      margin-top: 2rem;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 1rem;
      list-style: none;
      padding: 0;
    }
    .inside-highlights li {
      background: #f8fafc;
      border-radius: 8px;
      padding: 0.9rem 1.1rem;
      color: #334155;
      font-weight: 500;
      box-shadow: 0 6px 18px rgba(15, 23, 42, 0.05);
    }
    .gallery-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1rem;
      margin-top: 2rem;
    }
    .gallery-grid img {
      width: 100%;
      height: auto;
      border-radius: 10px;
      box-shadow: 0 10px 20px rgba(15, 23, 42, 0.12);
      background: #fff;
    }
    .emotional-section {
      margin: 3rem 0;
      background: #fff;
      border-radius: 12px;
      padding: 2.5rem;
      box-shadow: 0 12px 28px rgba(15, 23, 42, 0.07);
      line-height: 1.8;
      color: #334155;
      font-size: 1.08rem;
    }
    .reframe-table {
      margin: 3rem 0;
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(15, 23, 42, 0.08);
      overflow: hidden;
    }
    .reframe-table table {
      width: 100%;
      border-collapse: collapse;
    }
    .reframe-table th,
    .reframe-table td {
      padding: 1.25rem;
      text-align: left;
      border-bottom: 1px solid #e2e8f0;
      vertical-align: top;
      line-height: 1.6;
      color: #1f2933;
    }
    .reframe-table th {
      background: #2563eb;
      color: #fff;
      font-size: 1.05rem;
    }
    .reframe-table tr:last-child td {
      border-bottom: none;
    }
    .proof {
      margin: 3rem 0;
      display: grid;
      gap: 1rem;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    }
    .proof-item {
      background: #fff;
      border-radius: 10px;
      padding: 1.25rem;
      box-shadow: 0 6px 18px rgba(15, 23, 42, 0.06);
      font-weight: 600;
      color: #1f2937;
      text-align: center;
    }
    .cta-section {
      background: #1d4ed8;
      color: #fff;
      border-radius: 12px;
      padding: 3rem 2rem;
      text-align: center;
      margin: 3rem auto;
      box-shadow: 0 12px 30px rgba(37, 99, 235, 0.3);
    }
    .cta-section h2 {
      color: #fff;
      font-size: 2rem;
      margin-bottom: 1rem;
    }
    .cta-section p {
      color: #e2e8f0;
      font-size: 1.1rem;
      margin-bottom: 1.75rem;
    }
    .cta-section .buy-now { 
      background: #fff;
      color: #1d4ed8;
    }
    .cta-section .buy-now:not([disabled]):hover,
    .cta-section .buy-now:not([disabled]):focus {
      background: #e0e7ff;
    }
    .cta-section .buy-now[disabled] {
      background: rgba(255, 255, 255, 0.6);
      color: #1d4ed8;
    }
    .trust-icons {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      justify-content: center;
      margin-top: 1.5rem;
      font-weight: 600;
      color: #bfdbfe;
    }
    .final-cta {
      text-align: center;
      margin: 3rem auto 0;
    }
    .final-cta p {
      font-size: 1.15rem;
      color: #1f2933;
      margin-bottom: 1.5rem;
    }
    .final-cta .buy-now {
      padding: 0.9rem 2.5rem;
    }
    .guarantee-note {
      font-size: 0.95rem;
      color: #0f172a;
      margin-top: 1rem;
      font-weight: 500;
    }
    .testimonials {
      background: #fff;
      border-radius: 12px;
      padding: 3rem 0;
      box-shadow: 0 10px 30px rgba(15, 23, 42, 0.08);
      margin-bottom: 3rem;
    }
    .testimonials h2 {
      text-align: center;
      margin-bottom: 1.5rem;
    }
    .testimonial-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 1.5rem;
    }
    .testimonial {
      background: #f8fafc;
      border-radius: 10px;
      padding: 1.5rem;
      font-size: 1rem;
      line-height: 1.6;
      color: #1f2933;
    }
    .testimonial span {
      display: block;
      margin-top: 0.75rem;
      font-weight: 600;
      color: #2563eb;
      font-size: 0.95rem;
    }
    .faq {
      padding: 3rem 0;
    }
    .faq h2 {
      text-align: center;
      margin-bottom: 2rem;
    }
    .faq-item {
      background: #fff;
      border-radius: 10px;
      padding: 1.5rem;
      margin-bottom: 1rem;
      box-shadow: 0 6px 18px rgba(15, 23, 42, 0.06);
    }
    .faq-item h3 {
      margin-bottom: 0.5rem;
      font-size: 1.1rem;
    }
    .faq-item p {
      margin: 0;
      color: #475569;
      line-height: 1.6;
    }
    .sticky-bar {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      background: rgba(15, 23, 42, 0.95);
      color: #fff;
      padding: 0.75rem 1.25rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
      z-index: 30;
      flex-wrap: wrap;
    }
    .sticky-bar-info {
      display: flex;
      flex-direction: column;
      gap: 0.15rem;
    }
    .sticky-bar-price {
      font-weight: 600;
      font-size: 1rem;
    }
    .sticky-bar-title {
      font-size: 0.9rem;
      color: #cbd5f5;
    }
    .sticky-bar .buy-now {
      padding: 0.65rem 1.75rem;
      min-width: auto;
      font-size: 0.95rem;
    }
    @media (min-width: 1024px) {
      .hero {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }
    @media (min-width: 768px) {
      .sticky-bar {
        display: none;
      }
    }
    @media (max-width: 480px) {
      .hero {
        padding-top: 3rem;
      }
    }
  </style>
  {% if product == blank %}
    <div class="container">
      <p>Product not found. Please check back later.</p>
    </div>
  {% else %}
    <section class="hero container">
      <div class="hero-image">
        {% if product.featured_image %}
          <img src="{{ product.featured_image | image_url: width: 960 }}" alt="{{ product.title | escape }}" loading="lazy">
        {% endif %}
      </div>
      <div class="hero-content">
        <div class="hero-topline">
          <span class="hero-price-pill">{{ available_variant.price | default: product.price | money }}</span>
          <span>Instant PDF download — no subscription</span>
        </div>
        <h1>Therapist-Approved 3-in-1 Workbook — CBT • DBT • ACT.</h1>
        <p>Evidence-based tools that help clients regulate faster, understand the work visually, and keep your prep under five minutes.</p>
        <div class="hero-offer" role="region" aria-label="Workbook purchase details">
          <div class="hero-price">
            {{ available_variant.price | default: product.price | money }}
            <span>One-time professional license — yours to use forever.</span>
          </div>
          <p class="hero-license-note">One license covers one clinician with unlimited clients. Need a clinic license? <a href="/pages/contact">Contact us.</a></p>
          <button class="buy-now js-buy-now" type="button" data-variant-id="{{ available_variant.id }}" {% unless is_available %}disabled{% endunless %}>
            {% if is_available %}
              Download for {{ available_variant.price | default: product.price | money }} — Ready for Your Next Session
            {% else %}
              Sold Out
            {% endif %}
          </button>
          <p class="buy-now__message" role="status" aria-live="polite"></p>
          <p class="cta-reassurance">Instant PDF download after secure checkout — add it to your next client plan in minutes.</p>
          <a class="sample-link" href="{{ product.url }}?sample=pdf" target="_blank" rel="noopener">
            <svg aria-hidden="true" focusable="false" viewBox="0 0 20 20"><path d="M10 2a1 1 0 0 1 1 1v7.586l2.293-2.293a1 1 0 1 1 1.414 1.414l-4 4a1 1 0 0 1-1.414 0l-4-4A1 1 0 0 1 6.707 8.293L9 10.586V3a1 1 0 0 1 1-1Zm-6 11a1 1 0 0 1 1 1v1h10v-1a1 1 0 1 1 2 0v1a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-1a1 1 0 0 1 1-1Z"/></svg>
            Download 5 sample pages (PDF)
          </a>
        </div>
        <div class="hero-trust">
          <span>✔ Secure Checkout</span>
          <span>✔ Instant Download</span>
          <span>✔ Professional License</span>
          <span>✔ 7-day Therapist Guarantee</span>
        </div>
        <div class="hero-guarantee">
          <strong>Therapist Guarantee:</strong>
          If it doesn’t fit your style within 7 days, email us and we’ll make it right.
        </div>
        <div class="hero-guarantee">
          <strong>Therapist Guarantee:</strong>
          If it doesn’t fit your style within 7 days, email us and we’ll make it right.
        </div>
      </div>
    </section>

    <section class="benefits container">
      <h2>Why Therapists Love This Workbook</h2>
      <div class="benefit-grid">
        <div class="benefit-card">
          <h3>Prep every session in under five minutes.</h3>
          <p>Drop in ready-to-use prompts, agendas, and homework so you can focus entirely on clinical work.</p>
        </div>
        <div class="benefit-card">
          <h3>Visual tools clients instantly understand.</h3>
          <p>Use illustrated diagrams and language that explain complex concepts quickly and improve follow-through.</p>
        </div>
        <div class="benefit-card">
          <h3>CBT, DBT &amp; ACT workflows in one kit.</h3>
          <p>Move between modalities without juggling binders — each section includes step-by-step facilitator notes.</p>
        </div>
        <div class="benefit-card">
          <h3>Print, screen-share, or assign digitally.</h3>
          <p>Share worksheets in the format each client needs, from telehealth screen shares to in-office folders.</p>
        </div>
      </div>
      <blockquote>
        “This is the only workbook I need for new CBT/DBT clients.”
        <span>— Clinical Counselor, MA, LMHC</span>
      </blockquote>
    </section>

    {% assign gallery_images = product.media | where: 'media_type', 'image' %}
    <section class="gallery container" id="inside-workbook">
      <h2>Inside the Workbook</h2>
      <div class="inside-columns">
        <div>
          <h3>CBT — challenge automatic thoughts fast.</h3>
          <p>Lead clients through evidence-based thought records without writing new handouts from scratch.</p>
          <ul class="inside-bullets">
            <li>Printable worksheets</li>
            <li>Cognitive distortion cheat sheets</li>
            <li>Homework planners that boost follow-through</li>
          </ul>
        </div>
        <div>
          <h3>DBT — coach emotional regulation with confidence.</h3>
          <p>Drop in ready-to-teach skills labs that reinforce distress tolerance and interpersonal effectiveness.</p>
          <ul class="inside-bullets">
            <li>Diary cards clients actually complete</li>
            <li>Skills scripts for in-session practice</li>
            <li>Ready for teletherapy screen sharing</li>
          </ul>
        </div>
        <div>
          <h3>ACT — build psychological flexibility quickly.</h3>
          <p>Guide values work, defusion, and committed action with visuals that resonate in session.</p>
          <ul class="inside-bullets">
            <li>Values and commitment maps</li>
            <li>Mindfulness micro-practices</li>
            <li>Client reflection prompts for between sessions</li>
          </ul>
        </div>
      </div>
      <ul class="inside-highlights">
        <li>Ready for teletherapy and print</li>
        <li>Instant PDF download</li>
        <li>Editable fields for digital use</li>
        <li>Session recap notes for clinicians</li>
      </ul>
      {% if gallery_images.size > 0 %}
        <div class="gallery-grid">
          {% for media in gallery_images limit: 6 %}
            <div>
              <img src="{{ media | image_url: width: 720 }}" alt="Workbook preview {{ forloop.index }}" loading="lazy">
            </div>
          {% endfor %}
        </div>
      {% endif %}
    </section>

    <section class="container emotional-section">
      <p>Many clients come to therapy believing they are not enough. This workbook helps them rewrite that story — showing that healing begins with awareness, acceptance, and committed action.</p>
    </section>

    <div class="container reframe-table">
      <table>
        <thead>
          <tr>
            <th scope="col">Client Thought</th>
            <th scope="col">Therapist Reframe</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>“I’m not good enough.”</td>
            <td>“Let’s challenge that belief and see the evidence.”</td>
          </tr>
          <tr>
            <td>“If I can’t stop the panic, I’m failing therapy.”</td>
            <td>“Panic is a nervous system response; success is noticing it sooner and applying your skills.”</td>
          </tr>
          <tr>
            <td>“I always ruin relationships, so I should keep my distance.”</td>
            <td>“Your emotions signal core needs. Let’s practice DBT interpersonal effectiveness so you can connect without abandoning yourself.”</td>
          </tr>
          <tr>
            <td>“My thoughts are proof that I’m broken.”</td>
            <td>“Thoughts are mental events, not verdicts. We can observe them with ACT defusion and move toward the life you value.”</td>
          </tr>
        </tbody>
      </table>
    </div>

    <div class="container proof">
      <div class="proof-item">⭐ 4.4 / 5 on Amazon (14 reviews)</div>
      <div class="proof-item">📚 Used by thousands of therapists</div>
      <div class="proof-item">🧩 Published June 2021 — continually updated</div>
      <div class="proof-item">✍️ By David Lawson, PhD — specialist in emotional regulation</div>
    </div>

    <section class="testimonials container">
      <h2>Trusted by practicing therapists</h2>
      <div class="testimonial-grid">
        <div class="testimonial">
          “Excellent for brief interventions — it keeps session prep to minutes.”
          <span>— Dr. Alexis M., PsyD</span>
        </div>
        <div class="testimonial">
          “My CBT group loved the structure — the visuals made cognitive distortions click immediately.”
          <span>— Jordan L., LCSW</span>
        </div>
        <div class="testimonial">
          “Clients understood distortions instantly and stayed engaged between sessions.”
          <span>— Priya K., LMFT</span>
        </div>
        <div class="testimonial">
          “Perfect quick reference for trainees shadowing sessions.”
          <span>— Ethan R., LPC</span>
        </div>
      </div>
    </section>

    <section class="container cta-section">
      <h2>Ready to Simplify Your Sessions?</h2>
      <p>Download the professional therapist edition today and have it open for your next appointment.</p>
      <button class="buy-now js-buy-now" type="button" data-variant-id="{{ available_variant.id }}" {% unless is_available %}disabled{% endunless %}>
        {% if is_available %}
          Download for {{ available_variant.price | default: product.price | money }} — Ready for Your Next Session
        {% else %}
          Sold Out
        {% endif %}
      </button>
      <p class="guarantee-note">Therapist Guarantee: Try it for 7 days — if it doesn’t fit your approach, we’ll make it right.</p>
      <div class="trust-icons">
        <span>✔ Secure Checkout</span>
        <span>✔ Instant Download</span>
        <span>✔ Professional License</span>
      </div>
    </section>

    <section class="faq container">
      <h2>Frequently asked questions</h2>
      <div class="faq-item">
        <h3>Is it digital or physical?</h3>
        <p>Instant PDF download — ready for print or telehealth.</p>
      </div>
      <div class="faq-item">
        <h3>How does the license work?</h3>
        <p><strong>One clinician is fully covered.</strong> Use the workbook with unlimited clients forever. Need more seats? Reach out for clinic-wide options with volume pricing.</p>
      </div>
      <div class="faq-item">
        <h3>Is it good for trainees?</h3>
        <p>Perfect for supervised practice.</p>
      </div>
      <div class="faq-item">
        <h3>Can I get a clinic license?</h3>
        <p>Yes — email us to set up multi-therapist licensing with discounted volume pricing.</p>
      </div>
    </section>

    <div class="final-cta container">
      <p>Join thousands of therapists simplifying their sessions with this 3-in-1 workbook. Buy once — use it forever.</p>
      <button class="buy-now js-buy-now" type="button" data-variant-id="{{ available_variant.id }}" {% unless is_available %}disabled{% endunless %}>
        {% if is_available %}
          Download for {{ available_variant.price | default: product.price | money }} — Ready for Your Next Session
        {% else %}
          Sold Out
        {% endif %}
      </button>
      <p class="guarantee-note">Therapist Guarantee: Try it risk-free for 7 days — if it isn’t a match, email us and we’ll make it right.</p>
    </div>

    <div class="sticky-bar" {% unless is_available %}style="display:none;"{% endunless %}>
      <div class="sticky-bar-info">
        <span class="sticky-bar-title">{{ product.title }}</span>
        <span class="sticky-bar-price">{{ available_variant.price | default: product.price | money }}</span>
      </div>
      <button class="buy-now js-buy-now" type="button" data-variant-id="{{ available_variant.id }}" {% unless is_available %}disabled{% endunless %}>
        Download for {{ available_variant.price | default: product.price | money }}
      </button>
    </div>

    <script>
      document.addEventListener('DOMContentLoaded', function() {
        var buyButtons = document.querySelectorAll('.js-buy-now');
        var messageEl = document.querySelector('.buy-now__message');
        var isAvailable = {{ is_available | default: false | json }};

        if (!isAvailable) {
          buyButtons.forEach(function(button) {
            button.setAttribute('disabled', 'disabled');
          });
          return;
        }

        buyButtons.forEach(function(button) {
          button.addEventListener('click', function() {
            if (button.hasAttribute('disabled')) {
              return;
            }

            var variantId = button.getAttribute('data-variant-id');
            if (!variantId) {
              if (messageEl) {
                messageEl.textContent = 'Please try again.';
              }
              return;
            }

            buyButtons.forEach(function(btn) {
              btn.setAttribute('disabled', 'disabled');
            });
            if (messageEl) {
              messageEl.textContent = '';
            }

            fetch('/cart/add.js', {
              method: 'POST',
              headers: {
                'Content-Type': 'application/json',
                'Accept': 'application/json'
              },
              body: JSON.stringify({ id: variantId, quantity: 1 })
            })
              .then(function(response) {
                if (!response.ok) {
                  throw new Error('Network response was not ok');
                }
                return response.json();
              })
              .then(function() {
                window.location = '/checkout';
              })
              .catch(function() {
                if (messageEl) {
                  messageEl.textContent = 'Please try again.';
                }
                buyButtons.forEach(function(btn) {
                  btn.removeAttribute('disabled');
                });
              });
          });
        });
      });
    </script>

    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Product",
        "name": "{{ product.title | escape }}",
        "description": "{{ product.description | strip_html | truncate: 160 | escape }}",
        "image": [{% if product.featured_image %}"{{ product.featured_image | image_url: width: 960 }}"{% endif %}],
        "sku": "{{ available_variant.sku | escape }}",
        "offers": {
          "@type": "Offer",
          "priceCurrency": "{{ shop.currency }}",
          "price": "{{ available_variant.price | default: product.price | money_without_currency | strip_html | replace: ',', '' }}",
          "availability": "https://schema.org/{% if product.available %}InStock{% else %}OutOfStock{% endif %}"
        }
      }
    </script>
  {% endif %}
</div>
{% schema %}
{
  "name": "Therapist landing",
  "settings": []
}
{% endschema %}
